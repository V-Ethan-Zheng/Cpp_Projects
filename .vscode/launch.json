{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with CMake and lldb",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/${workspaceFolderBasename}", // 注意路径变化
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cmake: build"
        }
    ]
}